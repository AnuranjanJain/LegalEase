# Bytez API key - replace with a real key in your local .env (DO NOT commit)
BYTEZ_API_KEY=your_bytez_api_key_here

# Frontend dev URL used for CORS (default used in backend)
FRONTEND_URL=http://localhost:5173

# Optional: backend host/port or other feature flags
# PORT=8000
# STUB_MODE=true  # when set, backend returns deterministic stub responses
